{
  "name": "@openapps/web-worker",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "wrangler deploy --env=\"\" --dry-run --outdir dist",
    "typecheck": "tsc -p tsconfig.json",
    "cf:types": "wrangler types src/worker-configuration.d.ts",
    "cf:types:check": "wrangler types src/worker-configuration.d.ts --check",
    "deploy:dry": "wrangler deploy --env=\"\" --dry-run",
    "deploy": "wrangler deploy --env=\"\""
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "@openapps/core": "2.0.0",
    "@openapps/scrapers": "2.0.0",
    "hono": "4.12.34",
    "zod": "4.4.3"
  }
}
